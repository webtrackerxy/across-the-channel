import React from "react";
import ReactDOM from "react-dom/client";
import Entry from "./editorial/Entry";
import "./styles.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <Entry />
  </React.StrictMode>,
);
